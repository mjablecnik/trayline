# Taskline server URL (default: http://localhost:9090)
TASKLINE_URL=http://localhost:9090

# Bearer token to authenticate with the server, if it requires one (i.e. its
# APP_TOKEN is set). Leave unset for a purely local, unauthenticated server.
TASKLINE_TOKEN=
