variables:
  number: "1"
  path: "this"

steps:
  - name: "analyze-ui-structure"
    agent: "kiro"
    prompt: |
      FIRST: Check if .agents/tmp/TASKS.md already exists.
      If it exists AND contains at least one unchecked task (a line with "- [ ]"),
      then a previous run was interrupted before completing. Skip all analysis
      and just say "Skipping analysis — resuming from existing TASKS.md".
      Do NOT overwrite or modify the existing files.

      ONLY if TASKS.md does not exist or contains no unchecked tasks, proceed with the full analysis:

      Analyze the entire UI codebase in the {{path}} project directory.
      Detect the project framework (React, Svelte, Vue, Flutter, Next.js, Nuxt, SvelteKit, etc.)
      and examine all UI-related files — pages, components, layouts, sections, widgets.

      If AI_LOG.md exists inside .agents/, read it to understand what changed since the last
      ui-refactor run (new pages from design-to-code, new components, etc.). Focus analysis
      on what's new or changed rather than re-analyzing already refactored code.

      Your goal is to decompose every page into a clear hierarchy:
        Page → Sections → Components

      Additionally, extract a shared theme/design token layer:
      - Identify all colors, fonts, font sizes, font weights, spacing values, border radii,
        shadows, breakpoints, and other recurring visual constants used across the project.
      - Propose a centralized theme file (e.g., theme.ts, theme.dart, variables.css, tailwind.config.ts,
        or whatever is idiomatic for the detected framework) that defines all design tokens as variables.
      - All components, sections, and elements must reference these theme variables instead of
        hardcoding color hex codes, font names, pixel sizes, etc.
      - The theme must be structured so that changing a single variable (e.g., primary color, heading font)
        updates the entire application consistently.
      - If the framework supports theming natively (e.g., Flutter ThemeData, CSS custom properties,
        Tailwind config, MUI theme), use that mechanism.

      For each page:
      - Identify all distinct visual sections (hero, features, pricing, footer, etc.)
      - For each section, identify reusable UI components (buttons, cards, forms, navigation items, etc.)
      - Identify components and sections that appear on multiple pages — these must be shared, not duplicated

      Propose a folder structure following the conventions of the detected framework.
      The structure must be organized logically per page with a shared layer:

      - Each page gets its own folder containing its page-specific sections/ and components/
        Example (React-style): pages/home/sections/, pages/home/components/
        Example (SvelteKit-style): routes/(home)/sections/, routes/(home)/components/
      - Sections and components that belong only to one page live inside that page's folder
      - Sections and components used across multiple pages go into a shared/ folder
      - The smallest, most universal UI building blocks (buttons, inputs, badges, icons, etc.)
        that are used everywhere go into shared/elements/
      - If there are many elements, organize them into subfolders by function
        (e.g., shared/elements/buttons/, shared/elements/forms/, shared/elements/icons/, etc.)
        If only a few elements exist, a flat shared/elements/ folder is fine.
      - Use naming conventions idiomatic to the framework (e.g., PascalCase for React, kebab-case for Vue, etc.)
      - Pages should become short and readable — they import and compose sections, sections compose components

      Hierarchy:
        shared/elements/  → smallest reusable UI atoms (used everywhere), grouped into subfolders by function if many (buttons/, forms/, icons/, labels/, etc.)
        shared/components/ → components shared across multiple pages
        shared/sections/   → sections shared across multiple pages
        [page]/components/ → components specific to one page
        [page]/sections/   → sections specific to one page

      Create two files in .agents/tmp/ (create directories if they don't exist):

      1. REFACTORING.md — A detailed analysis document containing:
         - Overview of the current UI structure and detected framework
         - Theme/design token analysis:
           - All colors found in the codebase (hex values, named colors) and proposed token names
           - All fonts, font sizes, font weights, and line heights with proposed token names
           - Spacing values, border radii, shadows, and other recurring visual constants
           - Proposed theme file location and structure (idiomatic for the framework)
           - How components should reference theme tokens (imports, CSS variables, theme context, etc.)
         - For each page: list of sections it should be split into
         - For each section: list of components it should contain
         - Which sections and components are shared across multiple pages
         - Proposed folder structure with file paths for every new section and component
         - For each proposed section/component: name, purpose, which pages use it, and what props/parameters it needs
         - Sort by impact (most reused and largest sections first)

      2. TASKS.md — A task list derived from REFACTORING.md. Order tasks so dependencies are created first:
         Phase 0 — Create theme/design tokens:
         - [ ] 1. [HIGH] Create centralized theme file with all design tokens (colors, fonts, spacing, shadows, radii, breakpoints)
         - [ ] 2. [HIGH] Replace all hardcoded color values across the codebase with theme tokens
         - [ ] 3. [HIGH] Replace all hardcoded font families, sizes, and weights with theme tokens
         - [ ] 4. [MEDIUM] Replace all hardcoded spacing, border-radius, and shadow values with theme tokens
         Phase 1 — Create shared elements (smallest universal UI atoms):
         - [ ] 1. [HIGH] Create [element name] element in shared/elements/ — [purpose, used everywhere]
         Phase 2 — Create shared components and sections (used across multiple pages):
         - [ ] N. [HIGH] Create [component name] component in shared/components/ — [purpose, used by which pages]
         - [ ] N. [HIGH] Create [section name] section in shared/sections/ — [purpose, used by which pages]
         Phase 3 — Create page-specific components and sections:
         - [ ] N. [HIGH] Create [component name] component in [page]/components/ — [purpose]
         - [ ] N. [HIGH] Create [section name] section in [page]/sections/ — [purpose, composed of which components]
         Phase 4 — Refactor pages:
         - [ ] N. [MEDIUM] Refactor [page name] page to compose sections instead of inline code
         Final:
         - [ ] N. Verify all pages render correctly — no visual regressions, no duplicated code

         Each task must be a single, actionable change.

      Important:
      - The refactoring must NOT change the visual appearance of any page.
      - All visual constants (colors, fonts, spacing, shadows, radii) must be defined in the theme file.
      - Zero hardcoded visual values in components — everything references theme tokens.
      - The theme must be structured so changing one token updates the entire app consistently.
      - Pages must become short — they only import and compose sections.
      - Sections compose components. Components are the smallest reusable UI building blocks.
      - Zero duplicated UI code — if a section or component appears on multiple pages, it must be shared.
      - The final code must be clean, well-structured, and easily readable by a human developer.
      - Use clear, descriptive names following the framework's naming conventions.
      - Folder names (sections/, components/, elements/, shared/) should follow the framework's idiomatic structure.
      - If a component or section is only used by one page, it belongs in that page's folder, not in shared/.
      - Only truly shared parts go into shared/. Don't over-share — keep page-specific code with its page.

      STORYBOOK / COMPONENT CATALOG DETECTION:
      - Check if the project uses Storybook, Ladle, Histoire, or any similar component catalog tool.
        Look for config files like .storybook/, .ladle/, histoire.config.*, or related dependencies in package.json/pubspec.yaml.
      - If a component catalog tool is detected:
        - Document which tool is used and its configuration in REFACTORING.md.
        - For every component and element created in Phase 1-3, add a task to create a corresponding story file.
        - Stories must follow the conventions of the detected tool (e.g., ComponentName.stories.tsx for Storybook).
        - Stories should be placed according to the tool's conventions (co-located with components or in a stories/ folder).
        - Each story must showcase the component in its key states/variants (default, hover, disabled, with/without props, etc.)
        - Group stories logically: shared/elements, shared/components, page-specific components.
      - If NO component catalog tool is detected, skip story creation entirely — do not install one.

  - loop:
      max_iterations: 50
      condition:
        not_contains: "All tasks are done"
      steps:
        - name: "refactor-ui"
          agent: "claude"
          prompt: |
            Read REFACTORING.md and TASKS.md from .agents/tmp/.

            Your job is to refactor the UI code in the {{path}} project by decomposing pages
            into sections and components, eliminating all duplicated code.

            Rules:
            - Detect the project framework and follow its conventions for file naming,
              folder structure, imports, and component creation.
            - Use the centralized theme/design tokens for all visual values:
              - Never hardcode color hex codes, font names, pixel sizes, shadows, or radii in components.
              - Always reference theme variables/tokens defined in the theme file.
              - If a new visual value is needed, add it to the theme file first, then reference it.
              - The theme must be the single source of truth for all visual constants.
            - Organize code logically per page with a shared layer:
              - Each page has its own folder with page-specific sections/ and components/
              - Sections and components used across multiple pages go into shared/
              - The smallest universal UI atoms (buttons, inputs, badges, icons) go into shared/elements/
            - If a section or component is only used by one page, keep it in that page's folder.
            - Only truly shared parts go into shared/ — don't over-share.
            - Refactor pages to be short — they only import and compose sections.
            - Sections import and compose components. Components may use shared elements.
            - If a section or component is used on multiple pages, it must be a single shared file — no duplication.
            - The visual appearance of every page must remain exactly the same after refactoring.
            - Ensure all imports, routes, and navigation still work correctly after changes.
            - Follow the project's existing code style, linting rules, and conventions.
            - Do NOT add new features, pages, or functionality — only refactor existing code.
            - The result must contain zero duplicated UI code.
            - The result must be clean, well-structured, and easily readable by a human developer.
            - Use clear, descriptive names for components, sections, elements, props, variables, and files.
            - STORYBOOK / COMPONENT CATALOG:
              - If REFACTORING.md mentions a component catalog tool (Storybook, Ladle, Histoire, etc.),
                create story files for every component and element as specified in TASKS.md.
              - Follow the detected tool's conventions for file naming and placement.
              - Each story must render the component in its key visual states/variants.
              - Stories must use the centralized theme — no hardcoded values.
              - If no component catalog tool was detected, do NOT create any story files.

            Implement only up to {{number}} tasks in this run, then stop.
            After completing each task, check it off in .agents/tmp/TASKS.md.
            After every major change, create a git commit with name Martin Jablečník <martin.jablecnik@email.cz>.

            When you are done, print a summary of which tasks you completed.
            If all tasks in .agents/tmp/TASKS.md are now checked off, explicitly say "All tasks are done."
